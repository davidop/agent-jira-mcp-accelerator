---
phase: 04.2-container-apps-runtime-deployment-and-urls
plan: 01
type: execute
wave: 1
depends_on:
  - 04.1-01
files_modified:
  - infra/bicep/main.bicep
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - REQ-04
  - REQ-07
must_haves:
  truths:
    - Runtime infrastructure for Container Apps exists in Bicep with deploy toggle and image tag parameterization.
    - Deploy workflow runs runtime what-if and runtime deployment with deployApps=true.
    - Deploy workflow validates and prints non-empty service URLs for API, MCP, and Web.
  artifacts:
    - path: infra/bicep/main.bicep
      provides: runtime resources and URL outputs
    - path: .github/workflows/deploy.yml
      provides: runtime deployment orchestration and URL verification gate
  key_links:
    - from: .github/workflows/deploy.yml
      to: infra/bicep/main.bicep
      via: az deployment group what-if/create
      pattern: deployApps=true
    - from: infra/bicep/main.bicep
      to: .github/workflows/deploy.yml
      via: image tag and ingress URL outputs
      pattern: imageTag|ingress.fqdn
---

<objective>
Deploy runtime services on Azure Container Apps and expose concrete service URLs.

Purpose: Complete the transition from provisioned infra and published images to runnable cloud endpoints.
Output: Bicep runtime resources plus workflow runtime deployment and URL validation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.2-container-apps-runtime-deployment-and-urls/04.2-CONTEXT.md
@.planning/phases/04.2-container-apps-runtime-deployment-and-urls/04.2-RESEARCH.md
@infra/bicep/main.bicep
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Container Apps runtime resources and outputs in Bicep</name>
  <files>infra/bicep/main.bicep</files>
  <action>Ensure runtime resources include Log Analytics, managed identity, AcrPull assignment, Container Apps environment, and three service apps. Ensure image tag parameter controls runtime image references and outputs expose service URLs.</action>
  <acceptance_criteria>
    - Bicep declares managed environment and three container apps behind deploy toggle.
    - Runtime image references include repositories agent-api, jira-mcp-server, and web with parameterized tag.
    - Outputs include URL values for API, MCP server, and Web.
  </acceptance_criteria>
  <verify>
    <automated>rg "managedEnvironments|containerApps|agent-api|jira-mcp-server|web|imageTag|output agentApiUrl|output jiraMcpServerUrl|output webUrl" infra/bicep/main.bicep</automated>
  </verify>
  <done>Bicep runtime topology is declarative and release-tag aware.</done>
</task>

<task type="auto">
  <name>Task 2: Wire runtime what-if, deploy, and URL verification in deploy workflow</name>
  <files>.github/workflows/deploy.yml</files>
  <action>Ensure deploy workflow runs runtime what-if and runtime deployment using `deployApps=true` and SHA tag. Add a verification step that checks ingress FQDN for all three apps and prints final URLs.</action>
  <acceptance_criteria>
    - Workflow contains runtime what-if and runtime apply steps.
    - Runtime steps pass deploy toggle and image tag parameters.
    - URL verification fails when any FQDN is empty and prints URLs on success.
  </acceptance_criteria>
  <verify>
    <automated>rg "Runtime deploy what-if|Runtime deploy \(Container Apps\)|Verify runtime URLs|deployApps=true|imageTag=\"\$\{GITHUB_SHA\}\"|ingress\.fqdn" .github/workflows/deploy.yml</automated>
  </verify>
  <done>Deploy workflow can produce and validate runtime endpoints.</done>
</task>

</tasks>

<verification>
Run the Deploy to Azure workflow in GitHub Actions and confirm URLs are printed and reachable.
</verification>

<success_criteria>
Runtime deployment completes with non-empty URL outputs for API, MCP, and Web services.
</success_criteria>

<output>
Create `.planning/phases/04.2-container-apps-runtime-deployment-and-urls/04.2-01-SUMMARY.md` when done
</output>
