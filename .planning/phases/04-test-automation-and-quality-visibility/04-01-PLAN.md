---
phase: 04-test-automation-and-quality-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Jira.Client.Tests/Jira.Client.Tests.csproj
  - tests/Jira.Client.Tests/JiraCloudReaderResilienceTests.cs
  - tests/Agent.Api.Tests/Agent.Api.Tests.csproj
  - tests/Agent.Api.Tests/AgentAskEndpointTests.cs
  - tests/Jira.McpServer.Tests/Jira.McpServer.Tests.csproj
  - tests/Jira.McpServer.Tests/DemoEndpointsTests.cs
  - tests/Web.E2E/Web.E2E.csproj
  - tests/Web.E2E/SmokeJourneysTests.cs
  - tests/Fixtures/jira-mock-stable.json
autonomous: false
requirements:
  - REQ-04
  - REQ-06
  - REQ-07
must_haves:
  truths:
    - Mock-mode smoke tests cover blocked issues, sprint summary, and executive report journeys per D-01/D-03/D-05.
    - xUnit tests prove status normalization and resilience (retry, paging, rate-limit) for Jira.Client per D-08/D-10.
    - API and MCP seams remain read-only and deterministic while under test per D-09/D-16/D-17.
  artifacts:
    - path: tests/Web.E2E/SmokeJourneysTests.cs
      provides: Playwright happy-path smoke journeys
    - path: tests/Jira.Client.Tests/JiraCloudReaderResilienceTests.cs
      provides: deterministic resilience and mapping coverage
    - path: tests/Agent.Api.Tests/AgentAskEndpointTests.cs
      provides: ask endpoint contract tests for core demo prompts
    - path: tests/Jira.McpServer.Tests/DemoEndpointsTests.cs
      provides: MCP helper endpoint and read-only contract checks
    - path: tests/Fixtures/jira-mock-stable.json
      provides: shared deterministic fixture corpus for PR reliability
  key_links:
    - from: tests/Web.E2E/SmokeJourneysTests.cs
      to: src/Web/Program.cs
      via: browser interaction against Web to Agent.Api path
      pattern: api/agent/ask
    - from: tests/Jira.Client.Tests/JiraCloudReaderResilienceTests.cs
      to: src/Jira.Client/JiraCloudReader.cs
      via: fake HttpMessageHandler scenario matrix
      pattern: Retry-After|MaxPages|NormalizeStatus
    - from: tests/Agent.Api.Tests/AgentAskEndpointTests.cs
      to: src/Agent.Api/Services/DemoAgentService.cs
      via: request question routing assertions
      pattern: get_blocked_issues|get_sprint_summary
---

<objective>
Establish deterministic automated test coverage for the phase-4 critical paths without expanding product functionality.

Purpose: Enforce confidence in existing demo behavior with mock-backed Playwright smoke and xUnit layer tests.
Output: Test projects, deterministic fixtures, and executable tests for REQ-04/REQ-06/REQ-07.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-test-automation-and-quality-visibility/04-CONTEXT.md
@.planning/phases/04-test-automation-and-quality-visibility/04-RESEARCH.md
@src/Agent.Api/Services/DemoAgentService.cs
@src/Jira.Client/JiraCloudReader.cs
@src/Jira.McpServer/Tools/JiraTools.cs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify legitimacy of ASSUMED NuGet packages before installation</name>
  <read_first>
    - .planning/phases/04-test-automation-and-quality-visibility/04-RESEARCH.md
    - .planning/phases/04-test-automation-and-quality-visibility/04-CONTEXT.md
  </read_first>
  <action>Per the package legitimacy gate and research table, verify package identities on nuget.org for xunit, Microsoft.NET.Test.Sdk, xunit.runner.visualstudio, coverlet.collector, Microsoft.AspNetCore.Mvc.Testing, and Microsoft.Playwright.Xunit before any dotnet add package command runs; record confirmation in execution notes. This checkpoint is required because slopcheck was unavailable and packages are tagged ASSUMED.</action>
  <how-to-verify>
    1. Open each package page on nuget.org and confirm owner, package ID, and expected usage.
    2. Confirm no package is substituted with look-alike IDs.
    3. Resume only after all six entries are confirmed.
  </how-to-verify>
  <acceptance_criteria>
    - A verification note exists for all six package IDs.
    - No package install command has executed before this checkpoint approval.
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -Command "Select-String -Path '.planning/phases/04-test-automation-and-quality-visibility/04-RESEARCH.md' -Pattern 'xunit|Microsoft.NET.Test.Sdk|xunit.runner.visualstudio|coverlet.collector|Microsoft.AspNetCore.Mvc.Testing|Microsoft.Playwright.Xunit'"</automated>
  </verify>
  <resume-signal>Type approved when package legitimacy verification is complete, or list rejected package IDs.</resume-signal>
  <done>Package legitimacy verification is explicitly approved and the plan can proceed to install test dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold xUnit and Playwright test projects with deterministic fixture seam</name>
  <files>tests/Fixtures/jira-mock-stable.json, tests/Jira.Client.Tests/Jira.Client.Tests.csproj, tests/Jira.Client.Tests/JiraCloudReaderResilienceTests.cs, tests/Agent.Api.Tests/Agent.Api.Tests.csproj, tests/Agent.Api.Tests/AgentAskEndpointTests.cs, tests/Jira.McpServer.Tests/Jira.McpServer.Tests.csproj, tests/Jira.McpServer.Tests/DemoEndpointsTests.cs, tests/Web.E2E/Web.E2E.csproj, tests/Web.E2E/SmokeJourneysTests.cs</files>
  <read_first>
    - src/Jira.Client/JiraCloudReader.cs
    - src/Agent.Api/Services/DemoAgentService.cs
    - src/Agent.Api/Program.cs
    - src/Jira.McpServer/Program.cs
    - src/Jira.McpServer/Tools/JiraTools.cs
    - src/Web/Program.cs
    - .planning/phases/04-test-automation-and-quality-visibility/04-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: REQ-04 happy paths return expected tool hints and response shape for blocked issues, sprint summary, and executive report prompts.
    - Test 2: REQ-06 status normalization maps representative Jira statuses to deterministic internal values.
    - Test 3: REQ-07 paging and retry logic handles 429/5xx and Retry-After deterministically with fake HttpMessageHandler.
    - Test 4: Playwright smoke includes only three critical journeys and happy path assertions per D-01/D-02.
  </behavior>
  <action>Create four test projects under tests/ with shared deterministic fixtures per D-03. Implement Jira.Client tests first (D-08, D-10) with fake HttpMessageHandler matrices for pagination, retry, and mapping (REQ-06, REQ-07). Implement Agent.Api and Jira.McpServer in-memory integration tests for wiring and read-only behavior (D-09, D-16). Implement Playwright smoke tests in Mock mode only (D-05), with moderate timeout defaults and one retry (D-07), and include only blocked issues, sprint summary, and executive flow happy paths (D-01, D-02). Keep scope strictly validation of existing behavior (no new product capability) and do not introduce fixed coverage percentage gates in this phase (D-11).</action>
  <acceptance_criteria>
    - tests/Jira.Client.Tests contains deterministic tests for NormalizeStatus, paging loop boundaries, and Retry-After handling.
    - tests/Agent.Api.Tests validates ask endpoint behavior for the three critical prompt families and response schema.
    - tests/Jira.McpServer.Tests validates demo endpoints and read-only contracts without write operations.
    - tests/Web.E2E contains exactly three smoke journey tests aligned to D-01 and no negative-path scenarios.
    - All tests consume deterministic mock fixture data and do not require Jira Cloud credentials.
    - Plan defines critical-path verification only and introduces no fixed line or branch coverage threshold (D-11).
  </acceptance_criteria>
  <verify>
    <automated>dotnet test tests/Jira.Client.Tests/Jira.Client.Tests.csproj -c Release --filter "FullyQualifiedName~Normalize|FullyQualifiedName~Retry|FullyQualifiedName~Paging"</automated>
    <automated>dotnet test tests/Agent.Api.Tests/Agent.Api.Tests.csproj -c Release</automated>
    <automated>dotnet test tests/Jira.McpServer.Tests/Jira.McpServer.Tests.csproj -c Release</automated>
    <automated>dotnet test tests/Web.E2E/Web.E2E.csproj -c Release --filter "FullyQualifiedName~Smoke"</automated>
  </verify>
  <done>Layered test suites execute deterministically in local Mock mode and enforce phase-4 smoke and resilience coverage boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to web to api | Untrusted prompt text crosses into route handling and service routing |
| api or mcp to jira client seam | Tool invocations cross into data access abstraction and mapping logic |
| ci runner to artifacts | Test traces/logs cross into persisted build artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | test fixture corpus | mitigate | Use single immutable fixture corpus and deterministic IDs/timestamps referenced by all test layers |
| T-04-02 | Information Disclosure | CI test logs and traces | mitigate | Run only Mock-mode data in PR and avoid secret-bearing cloud calls in Playwright/xUnit artifacts |
| T-04-03 | Elevation of Privilege | MCP/API test surface | mitigate | Add explicit tests asserting read-only endpoints and absence of write paths per D-16 |
| T-04-SC | Tampering | NuGet package installs | mitigate | Blocking human legitimacy checkpoint for all ASSUMED packages before installation |
</threat_model>

<verification>
Run targeted layer commands for Jira.Client, Agent.Api, Jira.McpServer, and Web.E2E; ensure all smoke tests pass with no cloud dependency and no flaky quarantines.
</verification>

<success_criteria>
Deterministic tests for REQ-04/REQ-06/REQ-07 are implemented and executable in local Mock mode, with Playwright smoke bounded to the three locked journeys and happy-path-only scope.
</success_criteria>

<output>
Create `.planning/phases/04-test-automation-and-quality-visibility/04-01-SUMMARY.md` when done
</output>
