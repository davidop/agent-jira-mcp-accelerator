---
phase: 04.1-app-image-publication-to-acr
plan: 01
type: execute
wave: 1
depends_on:
  - 04-02
files_modified:
  - .github/workflows/deploy.yml
autonomous: true
requirements:
  - REQ-04
  - REQ-07
must_haves:
  truths:
    - Deploy workflow provisions base infra and then publishes three service images to ACR.
    - ACR repositories `agent-api`, `jira-mcp-server`, and `web` contain tags after publication.
    - Workflow fails when expected repository tags are missing.
  artifacts:
    - path: .github/workflows/deploy.yml
      provides: infra + image publication orchestration and verification
  key_links:
    - from: .github/workflows/deploy.yml
      to: src/Agent.Api/Agent.Api.csproj
      via: dotnet PublishContainer
      pattern: ContainerRepository="agent-api"
    - from: .github/workflows/deploy.yml
      to: src/Jira.McpServer/Jira.McpServer.csproj
      via: dotnet PublishContainer
      pattern: ContainerRepository="jira-mcp-server"
    - from: .github/workflows/deploy.yml
      to: src/Web/Web.csproj
      via: dotnet PublishContainer
      pattern: ContainerRepository="web"
---

<objective>
Publish deterministic container images for the three core services into ACR as a deployment artifact baseline.

Purpose: Ensure Azure resources are not only provisioned but backed by versioned runnable images.
Output: Updated deploy workflow that builds, pushes, and verifies ACR tags.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-app-image-publication-to-acr/04.1-CONTEXT.md
@.planning/phases/04.1-app-image-publication-to-acr/04.1-RESEARCH.md
@.github/workflows/deploy.yml
@infra/azure.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure deploy workflow can publish SDK containers to ACR</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml
    - infra/azure.yaml
    - .planning/phases/04.1-app-image-publication-to-acr/04.1-CONTEXT.md
  </read_first>
  <action>After provisioning and resource verification, authenticate to ACR and publish container images for Agent.Api, Jira.McpServer, and Web using `dotnet publish /t:PublishContainer` with `GITHUB_SHA` tag. Ensure each publish step uses explicit repository names and fails fast on errors.</action>
  <acceptance_criteria>
    - Workflow includes `actions/setup-dotnet@v4` for container publish support.
    - Workflow resolves ACR login server dynamically.
    - Workflow publishes three services with explicit repository names.
    - Image tag is commit-based (`GITHUB_SHA`).
  </acceptance_criteria>
  <verify>
    <automated>rg "Publish service images to ACR|PublishContainer|ContainerRepository=\"agent-api\"|ContainerRepository=\"jira-mcp-server\"|ContainerRepository=\"web\"" .github/workflows/deploy.yml</automated>
  </verify>
  <done>Deploy workflow reliably publishes tagged service images to ACR.</done>
</task>

<task type="auto">
  <name>Task 2: Add deployment gate for ACR tag visibility</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/deploy.yml
  </read_first>
  <action>Add post-publish verification that queries expected repositories and fails when no tags exist. Emit concise logs listing tags found for diagnostic traceability.</action>
  <acceptance_criteria>
    - Workflow checks `agent-api`, `jira-mcp-server`, and `web` repositories.
    - Missing tags produce non-zero exit code.
    - Successful runs print visible tag values in logs.
  </acceptance_criteria>
  <verify>
    <automated>az acr repository show-tags -n entagentaccelacr --repository agent-api -o table</automated>
    <automated>az acr repository show-tags -n entagentaccelacr --repository jira-mcp-server -o table</automated>
    <automated>az acr repository show-tags -n entagentaccelacr --repository web -o table</automated>
  </verify>
  <done>Registry visibility gate guarantees phase artifact integrity.</done>
</task>

</tasks>

<verification>
Run deployment workflow manually in GitHub Actions and confirm that ACR contains new tags for all three repositories.
</verification>

<success_criteria>
Infrastructure provisioning plus deterministic image publication to ACR complete with enforced repository tag visibility gate.
</success_criteria>

<output>
Create `.planning/phases/04.1-app-image-publication-to-acr/04.1-01-SUMMARY.md` when done
</output>
